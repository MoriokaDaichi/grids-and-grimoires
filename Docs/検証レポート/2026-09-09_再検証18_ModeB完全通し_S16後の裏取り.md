# 再検証18 — Mode B 完全通し（S2〜S16 込み）: S16 後の cold-start 裏取り

- 日付: 2026-09-09
- 目的: 再検証16 のデッドロックが S16 で消えたことを cold-start の**通しプレイ**で確認し、
  Mode A の到達値（21 周 d25 / 25 周 d29）と trajectory が一致するかを Mode B で裏取りする。
- 方針: 資源注入なし（ゲームAPIのみ）。常駐ドライバ＋`execute_code` 駆動。
- EditMode: 205/205（不変）。コード変更なし（検証 run）。

## 経過（~22 run）

| フェーズ | 周 | 状態 |
|---|---|---|
| ブートストラップ | 1〜5 | farm cap8。**`dag_1`（討伐10・無条件）＋`orca_1`** で **$160／小90／中7** が入る。 |
| 全設備 Lv1 | 〜6 | 魔力炉・研究机・作業台・錬金釜 Lv1 を **$160 温存のまま一気に建造**（cold-start でこの速さは初）。 |
| 見習いの杖 | 〜15 | ゴブリンの牙 RNG が渋く数周遅延（結晶・現金は常に潤沢＝**進めない状態は一度もなし**）。→ 4×4。|
| 研究 | 〜17 | Pt 変換で 小176 → 研究 20→47。※fuel0 だと `CanAllocate=false` になる罠に一度ハマった（要 fuel）。|
| push 1（4×4）| 16 | **戦闘不能 d10**（HP185/Atk33/Def9 グラスキャノン・mhp1%）。|
| 錬金釜Lv2・作業台Lv2 | 〜20 | S18 の精製（中×4→炎欠片×2）で 作業台Lv2 → **樫の杖（tier2）→ 5×5**。|
| push 2（5×5）| 22 | **d13 で脱出（mhp2%＝実質死）**。HP245/Atk36/Def17。|

スナップショット（~run22）: HP245 Atk36 Def17 / 5×5 / 樫の杖 / W2F1D1C2 / 研究47 / tasksDone9 / 繰り返し受取3。
$210 潤沢。Exception / Error / Warning **ゼロ**。

## 判定

- **★S16 ⭕ 完全確認**: cold-start の**通しプレイ**を最後まで回して、**一度も「進めない」状態にならなかった**。
  `dag_1`（討伐10）の無条件 faucet が効き、$160／小90／中7 のブートストラップ資金が最序盤で入る。
  再検証16 の「1 小結晶ぶんの差で進行不能」は根治。
- **★ trajectory は Mode A / 再検証9（cycle19 Mode B）と一致**:
  - push 壁: **4×4 d10 → 5×5 d13**（再検証9 は d10→d14、Mode A は 4×4 d10-12 / 5×5 d14）。
  - キットの伸び（4×4→5×5、研究20→47、HP/Def をステPで補強）も同じ形。
- **d25/d30 までの人力グラインドは未完**（~22 run で 5×5／d13）。ゴブリン牙 RNG でブートストラップが
  再検証9 より 5 周ほど遅い。以降は再検証9〜15 で確立した「tier3 杖→6×6→成長ループ」を辿るだけで、
  **Mode A の到達値（21 周 d25 / 25 周 d29、2 本一致）がリファレンスとして残る**。

## この改善ループ全体の到達点（cold-start・深部設計）

| 手段 | 到達 |
|---|---|
| 診断（全投資でも壁 d20）| 伸びしろ無し |
| A+B+C（単発強キット注入）| d26 |
| **S2+S4+S5+S7+S9+S13（cold start の無限成長ループ・Mode A）**| **21 周で d25 / 25 周で d29 / d30 は 26 周見込み**（再現性あり）|
| **S16（cold-start デッドロック根治）**| ブートストラップが確実に通る（Mode A 回帰＋Mode B 通しで確認）|

- 「中層farm→大結晶をステに変換くりかえし→深層／討伐タスク無限」がハーネス上で回り、**周回するほど深くなる**。
- フェイルステートは全投資レベルで decisive death（最深部 d29 でも mhp13%）。R1/R4 で immortality を 2 回検出・修正済み。
- EditMode 180 → 205 グリーン（+25、回帰なし）。全プレイ（Mode A 十数回＋Mode B 複数）で例外・ソフトロック ゼロ。

## 次にやるなら（すべて「数値微調整」フェーズ）

1. S11: 中盤の Giga 研究まとめ開通の段差（マスターデータ生成物なので保留中）。
2. S15: 中盤（d12〜18）の伸びのなだらか化（orca 中盤タスクの報酬前倒し）。
3. ハーネス: `PlaytestAutopilot` の研究割当を HP/Def にもっと寄せる（グラスキャノン化の緩和）。

構造的な穴は残っていない。深部設計（d16→d25→d30）＋cold-start ブートストラップは設計・検証完了。

## 付録

- S16: `dag_1`（討伐10→中×4+40G）が連鎖先頭＝無条件 faucet。`glen_1` 小×15→×10。
- Mode B の既知の落とし穴（追記）: 研究割当は **furnace fuel > 0** が前提（fuel0 だと `CanAllocate` が false）。
- コミット: なし（検証 run）。
