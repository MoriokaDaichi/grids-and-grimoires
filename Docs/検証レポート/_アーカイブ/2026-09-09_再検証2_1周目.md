# 再検証2 レポート — 1周目 / 全5周（改善「-4.」反映後）

- 日付: 2026-09-09
- 対象: DEV_LOG「-4.」の3改善 — **N1**（新規敵保証の副作用：深度2〜5で最低2体化＋1体ウェーブには保証を差さない）／**C1**（初期グリッド 2×2→**3×3**、tier1杖で **4×4**）／**C2**（深層スケーリングを更に寝かせる knee12→10・slope0.5→0.4／`orca_2b`「深度12到達」で `acc_regen_torc`＝毎秒+3.5回復を直接付与）
- 方針: 厳密に正攻法・資源注入なし。**セーブ wipe 後の cold start** から5周連続。
- EditMode テスト: **169/169 グリーン**（実行確認）
- 実行: 再生モード、`execute_code` 駆動・`Time.timeScale` 16〜30。戦利品は `RewardScreen.OnReturn` 明示呼び。

---

## 開始時（wipe 直後）

HP130 / Atk7 / Def5 / Spd5、未振りステP10、所持金 80 G、素材ゼロ、全設備Lv0、`bestDungeonDepth 0`。
**グリッドは初期から 3×3**（`minGridSize=3`。`WandTierToSize`：杖なし=3 / tier1杖=4 / tier2以上=5）。
**EnemyCountFor**：深度1=1体、深度2以降=2体（N1）。**深層スケーリング**：knee=10 / slope=0.4（C2）。

## 今周やったこと

1. ステ振り：HP+4 / Def+3 / Atk+3 → HP170 / Atk10 / Def8
2. **ブートストラップ出撃**（Flame＋Fire、3×3）→ 深度6脱出（HP117/170）
3. **N1 の効果確認**：farm 2回目・深度3 dash で **スライムゼリーが RNG 枠に復活**（前回検証では深度2〜5が敵1種固定で完全に枯れていた）。ただし debut 敵の保証が優先されるぶん Slime は依然「span 分の1」の確率で、furnace 用の×4 を集めるのに farm 3回＋深度3 dash 1回かかった
4. `dag_1`（討伐10）・`orca_1`（深度5）受領 → 中結晶×8 → 小結晶×80
5. **4設備すべて Lv1 建造**（魔力炉→燃料→錬金釜→変換→研究机→作業台）
6. 深度7 farm でゴブリンの牙補充（HP3/170 とかなり際どく脱出。深度6→7 の2体ウェーブが低HP時に65ほど削る）
7. **見習いの杖を製作**（小結晶×15 + ゴブリンの牙×3）→ `ApplyWandTierSize()` で **グリッド 3×3 → 4×4**（C1 動作確認）。Atk 13→15（杖 +3）
8. **研究22ノード**（Def/HP/Fire系）→ **MegaFire 解禁**。HP230 / Atk15 / Def12 / Spd6
9. **C1 の核心を検証**：4×4 に **MegaFire（L字4セル）＋ Flame（2×2 AoE）＋ Fire ＋ Wind** を**同時配置できた**（3×3 では物理的に不可能だった＝前回検証の壁）
10. **プッシュ出撃** → **深度11で戦闘不能**

## 1周目 終了時

| 項目 | 値 |
|---|---|
| プレイヤー | HP230 / Atk15 / Def12 / Spd6 / マナ120（サステイン未装備＝杖でゴブリンの牙を使い切った） |
| 所持金 | 75 G |
| ハイドアウト | 4設備 Lv1（燃料2）、マジックサークル未 |
| 研究 | 22ノード（MegaFire 含む） |
| グリッド | **4×4**、見習いの杖 |
| タスク完了 | dag_1, orca_1。`lifetimeEnemyKills 59` / `bestDungeonDepth 11` |

---

## 改善の中間評価（1周目時点）

| 改善 | 所見 |
|---|---|
| **C1（初期3×3＋tier1杖で4×4）** | ✅ **狙いどおり**。cold start でも 1周目で 4×4＝MegaFire＋Flame(AoE)＋単体2枚 が載る。前回検証は「3×3 で単体特化か範囲特化の二択」で3周かけて深度13頭打ちだった。火力ゲートは緩和された |
| **N1（スライム枯れ）** | ✅ 解消（Slime が再び抽選対象）。ただし依然「debut敵は保証／もう1枠がRNG」で、Slime は RNG 枠の 1/span。序盤の furnace 用スライムゼリー×4 集めはまだ少し手間（farm を数回） |
| **C2（深層スケーリング／`orca_2b`）** | 今周は深度11止まりで未検証。2周目以降で `orca_2b`（深度12到達→再生のトルク）と深度12〜14 の被弾を測る |

**到達深度**：1周目 深度11（HP230/Def12 と薄め）。C1 で「二択」は解けたが、HP/Def が育つのは2周目以降。

## 安定性
- 出撃6回・約35ウェーブ、設備建造4・製作1・研究22・トレード・脱出/死亡/報酬 — **例外・ソフトロック・進行不能なし**。
- グリッド 3×3→4×4 の動的リサイズ（`ResetBoard` 経由の盤面破棄→再構築）正常。
- 再生開始時 `EditorApplication.isPaused=true`（従来どおり）。

## 2周目の計画
1. `orca_2b`（深度12到達）を取りに行き **再生のトルク（毎秒+3.5）を入手 → C2 のサステイン底上げを検証**
2. サステイン装備の効果を tier1(1.5) と tier2(3.5) で比較
3. 研究を進めて HP/Def/GigaFire、魔力炉Lv2（鉄の兜）
4. プッシュで到達深度を再計測（4×4＋強サステインで深度12〜13 の即死が緩和されるか）
